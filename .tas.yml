---
framework: jest
postMerge:
  pattern:
    - "test/algorithms/geometric-test.js"
preMerge:
  pattern:
    - "test/algorithms/geometric-test.js"
preRun:
  command:
    - node --version
    - npm i && npm run react
postRun:
  command:
    - node --version
nodeVersion: 14.17.2
version: 2.1
